/*
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import {addNewTask} from './todosData'
import todosData from './todosData'

addNewTask('haha')

export let renderApp = (props) => {
    ReactDOM.render(
        <React.StrictMode>
            <App addNewTask={addNewTask}/>
        </React.StrictMode>,
        document.getElementById('root')
    );
}*/
